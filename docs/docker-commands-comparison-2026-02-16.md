# Docker 命令对比 - dev vs up-https

## 核心结论

- ⭐ **docker-dev**：本地开发，自动配置一切（mkcert + 构建 + 日志）
- 🚀 **docker-up-https**：通用 HTTPS 启动，需预先配置证书
- 📝 开发用 `docker-dev`，生产用 `deploy-https`
- 🔒 两者都支持 HTTPS，但证书来源不同

---

## 快速对比表

| 维度 | `make docker-dev` | `make docker-up-https` |
|------|------------------|----------------------|
| **适用场景** | 本地开发 ⭐ | 生产/已有证书环境 |
| **证书处理** | 自动生成 mkcert | 需预先存在证书 |
| **构建镜像** | ✅ 自动构建 | ❌ 不构建 |
| **启动服务** | ✅ 启动 HTTPS | ✅ 启动 HTTPS |
| **显示日志** | ✅ 实时日志 | ❌ 静默启动 |
| **域名检查** | ✅ 检查 local.yeanhua.asia | ❌ 不检查 |
| **域名使用** | local.yeanhua.asia | 任意（取决于证书）|
| **证书类型** | mkcert（本地 CA）| Let's Encrypt 或 mkcert |
| **使用频率** | 每天使用 | 首次启动/重启 |

---

## 详细对比

### `make docker-dev` - 本地开发（推荐）

#### 适用场景

- ✅ **日常本地开发**
- ✅ 首次启动项目
- ✅ 需要完整的开发环境
- ✅ 需要看到实时日志

#### 执行步骤

```bash
make docker-dev
```

**自动完成的操作**：

1. **检查本地域名配置**
   ```
   [1/4] 检查本地域名配置...
   ✓ local.yeanhua.asia 已配置
   ```

2. **检查/生成 mkcert 证书**
   ```
   [2/4] 检查/生成 mkcert 证书...
   ✓ 证书已存在
   ```
   - 如果证书不存在，自动运行 `mkcert` 生成
   - 使用本地 CA，浏览器自动信任

3. **构建 Docker 镜像**
   ```
   [3/4] 构建并启动服务（HTTPS）...
   ==> 构建 Docker 镜像...
   ✓ 构建完成
   ```

4. **启动 HTTPS 服务**
   ```
   ✓ 服务已启动
   ```

5. **显示实时日志**
   ```
   [4/4] 服务信息...
   ==> 查看实时日志 (Ctrl+C 退出)...
   nginx_1 | ...
   app_1   | ...
   ```

#### 访问地址

- https://local.yeanhua.asia 🔒
- https://localhost 🔒

#### 特点

- ✅ 一键启动，无需手动配置
- ✅ 自动处理证书
- ✅ 实时查看日志
- ✅ 适合快速迭代开发
- ✅ Ctrl+C 退出日志，服务继续运行

---

### `make docker-up-https` - HTTPS 服务启动

#### 适用场景

- ✅ **生产环境启动**
- ✅ 已有 Let's Encrypt 证书
- ✅ 只需启动服务，不需要构建
- ✅ 在后台运行，不需要看日志

#### 执行步骤

```bash
make docker-up-https
```

**执行的操作**：

1. **检查证书是否存在**
   ```
   ==> 检查 HTTPS 证书...
   ✓ 证书有效
   ```
   - 如果证书不存在，**会报错**
   - 需要先手动生成证书

2. **启动 HTTPS 服务**
   ```
   ==> 启动服务（HTTPS 模式）...
   ✓ 服务已启动（HTTPS）
   ```

3. **显示访问地址**
   ```
   访问地址:
     • https://local.yeanhua.asia  🔒
     • https://localhost  🔒
   ```

4. **显示服务状态**（不显示日志）

#### 访问地址

- 取决于证书配置的域名
- 可能是 `data.yeanhua.asia`（生产）
- 也可能是 `local.yeanhua.asia`（本地）

#### 特点

- ✅ 快速启动
- ✅ 静默运行
- ✅ 不重新构建
- ❌ 需要预先配置证书
- ❌ 不自动显示日志

---

## 使用场景决策

### 场景 1：本地日常开发（推荐）

```bash
# 每天开始工作
make docker-dev

# 修改代码后
vim main.go
make docker-rebuild

# 查看日志
make docker-logs
```

**为什么用 docker-dev**：
- ✅ 一键启动，无需关心证书
- ✅ 自动构建最新代码
- ✅ 实时查看日志

---

### 场景 2：生产环境首次部署

```bash
# 首次部署（推荐使用一键部署）
make deploy-https

# 或手动步骤
make cert-generate      # 生成 Let's Encrypt 证书
make docker-build       # 构建镜像
make docker-up-https    # 启动服务
```

**为什么用 docker-up-https**：
- ✅ 使用 Let's Encrypt 证书（生产级）
- ✅ 快速启动服务
- ✅ 后台运行

---

### 场景 3：生产环境重启服务

```bash
# 只需重启服务（不需要重新构建）
make docker-up-https

# 或
make docker-restart
```

**为什么用 docker-up-https**：
- ✅ 不重新构建，启动快
- ✅ 证书已存在

---

### 场景 4：本地测试 Let's Encrypt 证书

```bash
# 1. 生成 Let's Encrypt 证书（需要域名和 DNS）
make cert-generate-dns

# 2. 启动服务
make docker-up-https

# 访问
open https://data.yeanhua.asia
```

**为什么用 docker-up-https**：
- ✅ 测试真实的 Let's Encrypt 证书
- ✅ 模拟生产环境

---

## 命令组合使用

### 开发流程

```bash
# 1. 启动开发环境
make docker-dev

# 2. 修改代码（在另一个终端）
vim main.go

# 3. 重新构建并重启
make docker-rebuild

# 4. 继续查看日志（如果需要）
make docker-logs
```

### 生产部署流程

```bash
# 1. 一键部署（推荐）
make deploy-https

# 或手动步骤
# 2a. 生成证书
make cert-generate

# 2b. 构建镜像
make docker-build

# 2c. 启动服务
make docker-up-https
```

### 快速重启

```bash
# HTTP 模式
make docker-up

# HTTPS 模式（已有证书）
make docker-up-https

# 重启现有服务
make docker-restart
```

---

## 证书来源对比

### docker-dev 使用的证书

**类型**：mkcert（本地 CA）

**特点**：
- ✅ 自动生成
- ✅ 本地浏览器信任
- ✅ 无需域名
- ✅ 长期有效（10 年）
- ❌ 仅本机信任
- ❌ 不能用于生产

**生成**：
```bash
# docker-dev 自动执行
mkcert local.yeanhua.asia localhost 127.0.0.1 ::1
```

**使用的域名**：
- local.yeanhua.asia
- localhost

---

### docker-up-https 使用的证书

**类型**：Let's Encrypt 或 mkcert（预先生成）

**特点**：
- ✅ 全球浏览器信任（Let's Encrypt）
- ✅ 可用于生产
- ✅ 支持真实域名
- ❌ 需要手动生成
- ❌ 90 天有效期（Let's Encrypt）

**生成方式**：

1. **Let's Encrypt HTTP-01**（生产环境）：
   ```bash
   make cert-generate
   ```

2. **Let's Encrypt DNS-01**（泛域名）：
   ```bash
   make cert-generate-dns
   ```

3. **mkcert**（本地开发）：
   ```bash
   make cert-generate-mkcert
   ```

**使用的域名**：
- 取决于证书配置
- 可以是 data.yeanhua.asia（生产）
- 也可以是 local.yeanhua.asia（本地）

---

## 常见问题

### Q1: docker-dev 和 docker-up-https 有什么区别？

**简答**：
- `docker-dev`：开发专用，自动配置一切
- `docker-up-https`：通用启动，需预先配置

**详细**：

| 方面 | docker-dev | docker-up-https |
|------|-----------|----------------|
| 构建镜像 | ✅ | ❌ |
| 生成证书 | ✅ 自动（mkcert）| ❌ 需预先存在 |
| 显示日志 | ✅ 实时 | ❌ 静默 |
| 域名检查 | ✅ | ❌ |

---

### Q2: 什么时候用 docker-dev？

**使用 docker-dev**：
- ✅ 本地日常开发
- ✅ 首次启动项目
- ✅ 需要快速迭代
- ✅ 需要看到实时日志

**命令**：
```bash
make docker-dev
```

---

### Q3: 什么时候用 docker-up-https？

**使用 docker-up-https**：
- ✅ 生产环境
- ✅ 已有证书
- ✅ 只需启动服务
- ✅ 后台运行

**命令**：
```bash
# 前提：证书已存在
make docker-up-https
```

---

### Q4: docker-dev 能在生产环境用吗？

**不推荐**。

**原因**：
- ❌ 使用 mkcert 证书（仅本机信任）
- ❌ 每次都构建镜像（慢）
- ❌ 显示日志（占用终端）

**生产环境推荐**：
```bash
# 使用一键部署
make deploy-https

# 或
make cert-generate     # Let's Encrypt
make docker-build
make docker-up-https
```

---

### Q5: 如何从 docker-dev 切换到生产模式？

```bash
# 1. 停止开发环境
make docker-down

# 2. 清理 mkcert 证书
make cert-clean

# 3. 生成 Let's Encrypt 证书
make cert-generate      # HTTP-01
# 或
make cert-generate-dns  # DNS-01

# 4. 启动生产环境
make docker-up-https
```

---

### Q6: docker-up-https 能显示日志吗？

可以，但需要单独运行：

```bash
# 启动服务（后台）
make docker-up-https

# 查看日志（单独命令）
make docker-logs
```

---

### Q7: 两个命令能同时用吗？

**不能**。它们都会占用相同的端口（80/443）。

**正确做法**：
```bash
# 先停止现有服务
make docker-down

# 再启动新服务
make docker-dev
# 或
make docker-up-https
```

---

## 命令速查

### 快速决策

```bash
# 本地开发？
→ make docker-dev

# 生产首次部署？
→ make deploy-https

# 生产重启服务？
→ make docker-up-https

# 代码更新？
→ make update-https
```

### 完整命令表

| 场景 | 命令 | 说明 |
|------|------|------|
| **本地开发** | `make docker-dev` | 自动一切 |
| **生产首次** | `make deploy-https` | 一键部署 |
| **生产启动** | `make docker-up-https` | 启动 HTTPS |
| **HTTP 启动** | `make docker-up` | 启动 HTTP |
| **停止服务** | `make docker-down` | 停止所有 |
| **重启服务** | `make docker-restart` | 重启 |
| **查看日志** | `make docker-logs` | 实时日志 |
| **查看状态** | `make docker-ps` | 容器状态 |

---

## 最佳实践

### 本地开发

```bash
# 每天开始
make docker-dev         # 一键启动

# 修改代码后
make docker-rebuild     # 重新构建

# 需要时查看日志
make docker-logs        # 实时日志
```

### 生产部署

```bash
# 首次部署
make deploy-https       # 一键部署

# 代码更新
make update-https       # 一键更新

# 仅重启服务
make docker-restart     # 快速重启
```

---

## 相关文档

- [本地开发指南](local-dev-guide-2026-02-16.md) - docker-dev 详细说明
- [代码更新指南](code-update-guide-2026-02-16.md) - 生产环境部署
- [快速参考](quick-reference-2026-02-16.md) - 所有命令速查

---

## 总结

### 核心差异

| 命令 | 用途 | 证书 | 构建 | 日志 |
|------|------|------|------|------|
| **docker-dev** | 本地开发 | 自动 mkcert | ✅ | ✅ 实时 |
| **docker-up-https** | 通用启动 | 需预先存在 | ❌ | ❌ 静默 |

### 选择建议

- 🛠️ **本地开发** → `make docker-dev`
- 🚀 **生产部署** → `make deploy-https`
- 🔄 **生产重启** → `make docker-up-https`

### 牢记

- `docker-dev` = 开发专用，自动一切 ⭐
- `docker-up-https` = 通用启动，需预先配置
- `deploy-https` = 生产一键部署（推荐）⭐
